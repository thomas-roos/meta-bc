From: Q CLI <q@amazon.com>
Subject: Fix git describe failure when tags are not available
Upstream-Status: Inappropriate [embedded specific]

--- a/bctoolbox/cmake/BCToolboxCMakeUtils.cmake
+++ b/bctoolbox/cmake/BCToolboxCMakeUtils.cmake
@@ -141,7 +141,10 @@
 			WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
 		)
 		if(NOT GIT_DESCRIBE_STATUS EQUAL 0)
-			message(FATAL_ERROR "fail to get GIT describe version")
+			message(WARNING "fail to get GIT describe version, using fallback")
+			set(GIT_DESCRIBE_VERSION "5.3.0")
+			set(GIT_DESCRIBE_STATUS 0)
+			# message(FATAL_ERROR "fail to get GIT describe version")
 		endif()
 
 		# parse git describe version
